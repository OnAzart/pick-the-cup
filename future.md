# Future work

1. Add match dates to the bracket (kickoff times per match, so users know when each game is played).
2. Highlight what's wrong — visually flag picks that turned out incorrect once the real result is locked in (not just showing the locked real result, but marking the user's own wrong guess).
3. Add a leaderboard (rank users by bracket accuracy once emails/predictions are persisted).
4. Add scoring (points per correct pick, likely weighted by round — e.g. group winners worth less than a correct Final pick).
